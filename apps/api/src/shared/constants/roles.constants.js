/**
 * USER_ROLES — single source of truth for role strings.
 * Usage: import { USER_ROLES } from '../../shared/constants/roles.constants.js'
 */
export const USER_ROLES = Object.freeze({
    CLIENT: "CLIENT",
    DEVELOPER: "DEVELOPER",
    BOTH: "BOTH",
    ADMIN: "ADMIN",
});
